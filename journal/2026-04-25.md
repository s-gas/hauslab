# 25-04-2026

Started working on gasbot, a home gas meter tracker via Telegram. The idea is to send the readings to a Telegram bot and then the Go app reads it, validates it and stores it in a database.

Started by creating the telegram bot. Since there is a token, would be smart to start by creating a docker-compose and store it as secret.

=====

Found a Go library to use as API for the telegram bot, but it uses channels, which means that I have to learn how channels work in Go.

=====

Also realized that all the logs have the wrong hour cause I haven't set the environment variable `TZ`.

=====

Finally the HDMI to DisplayPort arrived. I finally installed Debian on the homelab and decided not to install a GUI since i'm gonna access it only via SSH. I also set its internal IP address to be fixed to `192.168.178.2` so it's easier to find.
