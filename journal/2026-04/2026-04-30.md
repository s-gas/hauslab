# 30-04-2026

I had a look at what channels are in Go and how they work with `goroutines`. I guess now I can try to build the gasbot, which i'm gonna rename to *gasmetrics*.

======

Managed to set up the service so that it reads the message and send a reply back. The next step is probably input validation and then I'll start thinking about the database. Not sure if it's better to have a database that connects to all the services that need to use it or having a specific database just for this service.
