# 18-04-2026

Still wondering if it's better to dockerize self-built services without dependencies or not.
I guess I'll figure it out whenever I'll hit some friction.

I think for now the priority is actually build other services.

Another priority might be to replace the map in **svcmonitor** with a map defined through a config file, so that i dont have to recompile every time I add a service that needs monitoring.