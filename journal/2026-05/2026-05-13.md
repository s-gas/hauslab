# 13-05-2026

**gasmetrics** is taking shape! I basically forgot how to run queries and exec in SQL so I should review that in these days.
Also managed to split the code in different packages, each in its own directory and following the convention of having `main` in `cmd/` and the rest of the code in `internal`. I definitely like this split and how everything is more organized, especially if inside `internal` I keep different directories for packages with various responsabilities.

=====

Once I get home I should try to create the Grafana dashboard for **gasmetrics**.
Or maybe I should start considering provisioning, so that i can define the dashboards in `yaml` files.
