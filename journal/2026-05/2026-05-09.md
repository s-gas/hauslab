# 09-05-2026

When i get home I should change how i return errors from functions. instead of simply returning "err" i should wrap it in fmt.Errorf() so i can specify in what function that error happened. I'm also reading that the error should use %w as format specifier when returning it, in this way the error is "wrapped", which means that the error type is preserved.