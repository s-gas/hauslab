# 08-05-2026

So far I have been using `admin` as username in my services, which is definitely bad practice.
I'm gonna delete all volume data and add the user environment variables to the services that need them.

=====

I also created a new email address `s.gas.hauslab@gmail.com` that I'm going to use specifically for the homelab.

=====

I'm realizing that having so many networks does not guarantee isolation. The problem is that I have some networks that reach basically all services, which means that all services can communicate between each other.
I'm gonna simplify everything by having a single network for now which I'm gonna simply call `hauslab`.
