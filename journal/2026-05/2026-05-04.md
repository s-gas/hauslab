# 04-05-2026

Today I added Nginx. So far it wasn't necessary since I didn't have many ports exposed, but I did it as an exercise.

As first approach I tried to configure nginx with subpaths but that required modifying environment variables for each service, so I decided to use subdomains instead, which turned out to be much simpler.

So far I added subdomains only for Prometheus and Grafana. I wanted to add it also for Adguard, but both services listen on port 80 so I couldn't. The solution would be to use Nginx only with HTTPS, but that would require SSL certificate and the self-signed ones would show a security warning on the browser.

======

Actually that was a big lie! There is no port conflict if I don't expose the AdGuard Web UI to the host!
Let's add AdGuard as a subdomain!

======

Added AdGuard's subdomain and removed the exposed ports to the host.
