services:
  svcmonitor:
    build: .
    volumes:
      - ./svcmonitor.log:/usr/src/app/svcmonitor.log
    networks:
      - monitor
    restart: unless-stopped

networks:
  monitor:
    external: true
