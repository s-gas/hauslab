name: svcmonitor

services:
  svcmonitor:
    build: .
    container_name: svcmonitor
    volumes:
      - ./services.yaml:/usr/src/app/services.yaml
    environment:
      - TZ=Europe/Berlin
    networks:
      - hauslab
    restart: unless-stopped

networks:
  hauslab:
    external: true
