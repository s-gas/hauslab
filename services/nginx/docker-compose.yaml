name: nginx

services:
  nginx:
    image: nginx:1.29.8
    container_name: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./index.html:/usr/share/nginx/html/index.html
    ports:
      - "80:80"
    networks:
      - proxy

networks:
  proxy:
    external: true
