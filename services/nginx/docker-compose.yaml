name: nginx

services:
  nginx:
    image: nginx:1.29.8
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "80:80"
    networks:
      - proxy

networks:
  proxy:
    external: true
