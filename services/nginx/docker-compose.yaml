name: nginx

services:
  nginx:
    image: nginx:1.29.8
    container_name: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./www/:/usr/share/nginx/html/
    ports:
      - "80:80"
    networks:
      - hauslab

networks:
  hauslab:
    external: true
